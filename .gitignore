# Upstream clones — fetched by scripts/sync.sh, never edited, never committed.
upstream/
# Generated by scripts/build.sh from upstream/ + overlay/.
build/
# Eval run artifacts (transcripts); summaries under eval/results/ are committed.
eval/results/raw/
.DS_Store
.install-state.json
eval/fixture/.git
